import React from 'react';
import { Form } from 'antd';

const FormItem = Form.Item;

export default ({
  form,
  layout,
  formName,
  record,
  onFinish,
  fields,
  body,
  setBody,
  headers,
  setHeaders,
}) => {
  return <div></div>;
};
